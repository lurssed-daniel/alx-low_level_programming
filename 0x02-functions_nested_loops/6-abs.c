#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @c: The character to check
 *
 * Return: computed value.
 */
int _abs(int c)
{
if (c < 0)
return (c * (-1));
return (c);

}
